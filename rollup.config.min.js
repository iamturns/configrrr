import babel from 'rollup-plugin-babel';
import uglify from 'rollup-plugin-uglify';

export default {
	entry: 'src/index.js',
	dest: 'index.min.js',
	format: 'umd',
	moduleName: 'configrrr',
	plugins: [
		babel(),
		uglify()
	],
};
